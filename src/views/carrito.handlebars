<div class="container mt-5 mb-5">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title text-center">Detalle del Carrito</h5>
            <ul class="list-group">
                {{#each products}}
                    <li class="list-group-item bg-dark text-light d-flex justify-content-between align-items-start w-50 mx-auto rounded">
                        <span><strong>{{this.title}}</strong></span>
                        <span>X <strong>{{this.qty}}</strong></span>
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
    <p>*La funcion de obtener el carrito por user especifico todavia en desarrolo*</p>
</div>