<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beyond Games</title>
    <link rel="shortcut icon" href="/static/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="/static/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    {{#if style}} 
        <link rel="stylesheet" href="/static/css/{{style}}.css" type="text/css">
    {{/if}}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-dark mb-5">
        <div class="container">
            <a class="navbar-brand fs-2 bold fw-bold text-light" href="/">BEYOND GAMES</a>
            <button class="navbar-toggler bg-light rounded" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item dropdown-center">
                        <button class="btn dropdown-toggle text-light fs-5" type="button" data-bs-toggle="dropdown" aria-expanded="false">Categorias</button>
                        <ul class="dropdown-menu rounded">
                            <li><a class="dropdown-item" href="/">Todos los juegos</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/categoria/FPS">FPS</a></li>
                            <li><a class="dropdown-item" href="/categoria/accion">Acción</a></li>
                            <li><a class="dropdown-item" href="/categoria/aventura">Aventura</a></li>
                            <li><a class="dropdown-item" href="/categoria/exploracion">Exploración</a></li>
                            <li><a class="dropdown-item" href="/categoria/multijugador">Multijugador</a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light fs-5" href="/cart">
                            Carrito
                            <i class="bi bi-bag"></i>
                        </a>
                    </li>
                    {{#if user}}
                    <li class="nav-item dropdown-center">
                        {{#if user.isUser}}
                        <button class="btn dropdown-toggle text-light fs-5" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Bienvenido, {{user.firstname}}!
                        </button>
                        {{else if user.isAdmin}}
                        <button class="btn dropdown-toggle text-light fs-5" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Bienvenido, Admin!
                        </button>
                        {{/if}}
                        <ul class="dropdown-menu rounded">
                            <li>
                                <a class="dropdown-item" href="/profile">Perfil</a>
                            </li>
                            {{#if user.isAdmin}}
                                <li>
                                    <a class="dropdown-item" href="/">Admin</a>
                                </li>
                            {{else if user.isUser}}
                                <li>
                                    <a class="dropdown-item" href="/">Usuario</a>
                                </li>
                            {{/if}}
                            <li>
                                <a class="dropdown-item" href="/logout">Logout</a>
                            </li>
                        </ul>
                    </li>
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link text-light fs-5" href="/login">Iniciar Sesion</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        {{{body}}}
    </div>

    {{#if showChatIcon}}
        <div class="position-fixed bottom-0 start-0 mx-3 msg-animation">
            <a href="/chat">
                <i class="bi bi-chat-dots-fill fa-lg text-success display-3"></i>
            </a>
        </div>
    {{/if}}
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.0.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>